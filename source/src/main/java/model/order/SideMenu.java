package model.order;

public class SideMenu {
    // Fields: menu(String), price(int), sale(float), amount(int)
    private String menu;
    private int price;
    private float sale;
    private int amount;
}
