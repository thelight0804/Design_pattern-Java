package model.order;

public class Alcohol {
    // Fields: menu(string), price(int), sale(float), amount(int)
    private String menu;
    private int price;
    private float sale;
    private int amount;
}
